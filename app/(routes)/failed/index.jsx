import React from 'react';
import { StyleSheet, View } from 'react-native';
import Faileds from '../../../screens/Failed/Faileds';

const Failed = () => {
    return (
       <Faileds/>
    );
}

const styles = StyleSheet.create({})

export default Failed;
