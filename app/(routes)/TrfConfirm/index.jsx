import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import Confirm from '../../../screens/Confirms/Confirm';

const Confirmations = () => {
    return (
          <Confirm/>
    );
}

const styles = StyleSheet.create({})

export default Confirmations;
